<?php

return [

	/**
	 * Base path
	 * *********
	 * 
	 * This is required for the router to work properly.
	 * If "public" folder is sitting under the root folder of a domain, define path here,
	 * otherwise, leave empty.
	 *
	 * Example 1:
	 * Public folder is accessed as follows: 'http://127.0.0.1/repos/phptest/public/'
	 * set "base_path" variable to "/repos/phptest/public" (no trailing slash).
	 *
	 * Example 2:
	 * If public folder is accessed as follows: 'http://phptest.local/' then leave
	 * "base_path" empty.
	 */
	
	"base_path" => ""


];